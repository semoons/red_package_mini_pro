<view class="tab_bar">
    <navigator
		class="tab_item"
		wx:for="{{tabBarList}}"
		url="{{item.path}}"
		wx:key="{{index}}">
        <image src="{{item.iconUrl}}" mode="widthFix" class="tab_icon"></image>
        <view class="tab_text">{{item.text}}</view>
    </navigator>
</view>